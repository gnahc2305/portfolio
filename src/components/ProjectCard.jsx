import React from 'react'

function ProjectCard({ project: {title, gif, description, stack, code, live} }) {
  return (
    <div className='mt-[120px]'>
        Project
    </div>
  )
}

export default ProjectCard